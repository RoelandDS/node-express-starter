extends layout

block content
  .page-header
    h1 Our blogs

  for blog in blogs
    .panel.panel-default
      .panel-heading
        h3.panel-title
          a(href='/blog/'+blog._id) #{blog.title}
      .panel-body
        | #{blog.blogBody}
      .panel-footer
        | #{blog.name}